# Cloudflare Pages redirect rules
# https://developers.cloudflare.com/pages/configuration/redirects/
#
# Note: _redirects matches path only. Host-based redirects (www -> apex) must
# be set as a zone-level Cloudflare Redirect Rule. Currently both www and apex
# serve identical content with <link rel="canonical"> pointing to apex, which
# Google consolidates correctly.
