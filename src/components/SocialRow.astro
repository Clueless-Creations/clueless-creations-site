---
const links = [
  { href: 'https://x.com/EduMuthMartinez', label: 'X / Twitter', icon: 'x' },
  { href: 'https://github.com/Clueless-Creations', label: 'GitHub (Clueless Creations org)', icon: 'github' },
  { href: 'https://www.linkedin.com/in/eduardomuthmartinez/', label: 'LinkedIn', icon: 'linkedin' },
  { href: 'https://www.thecompoundingfounder.com/', label: 'The Compounding Founder', icon: 'substack' },
];
---
<ul class="social-row">
  {links.map(link => (
    <li>
      <a href={link.href} aria-label={link.label} target="_blank" rel="noopener" class="social-pill">
        <svg width="18" height="18" aria-hidden="true" focusable="false">
          <use href={`/icons/social.svg#${link.icon}`} />
        </svg>
      </a>
    </li>
  ))}
</ul>

<style>
  .social-row {
    display: flex;
    gap: 0.75rem;
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .social-pill {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    height: 44px;
    border-radius: var(--radius-full);
    background: rgba(205, 221, 255, 0.08);
    color: var(--color-fg);
    text-decoration: none;
    transition: background 0.2s ease, color 0.2s ease;
  }
  .social-pill:hover {
    background: rgba(205, 221, 255, 0.18);
    color: var(--color-fg);
  }
  .social-pill svg { color: inherit; }
</style>
